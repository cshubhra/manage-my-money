package com.finance.application.model;

/**
 * Enum representing the type of a transfer item.
 */
public enum TransferItemType {
    /**
     * Income type represents money coming in (positive value).
     */
    INCOME,
    
    /**
     * Outcome type represents money going out (negative value).
     */
    OUTCOME
}